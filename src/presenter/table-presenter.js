export default function buildTablePresenter() {
    /**
     * raw data should be an object
     */
    return function(rawData,arr) {  
        for(let dep in rawData) {
            
        }
    }
}